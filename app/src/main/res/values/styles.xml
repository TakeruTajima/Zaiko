<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textSize">@dimen/sizeTextSecondary</item>
        <item name="textColorPrimary">@color/colorTextPrimary</item>
        <item name="textColorSecondary">@color/colorTextSecondary</item>
        <item name="textColorLink">@color/colorTextLink</item>
        <item name="textSizeHeadline">@dimen/sizeTextPrimary</item>
        <item name="textSizeBody">@dimen/sizeTextSecondary</item>
        <item name="textSizeMinimum">@dimen/sizeTextMinimum</item> <!-- 4の倍数な -->
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>



</resources>
